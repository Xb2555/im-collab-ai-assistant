  禁止任何业务硬编码。
  禁止出现这类实现：

  - if (text.contains("开头"))
  - if (text.contains("小节"))
  - if (text.contains("章节"))
  - if (text.equals("作者信息"))
  - if ("document_head".equals(...)) 这种把业务语义写死在代码里的分支
  - 任何靠中文词面、固定措辞、特定标题样式来决定编辑语义/锚点/patch 类型的逻辑

  允许保留的只有：

  - 通用结构解析
  - 通用 XML/Markdown 解析
  - 通用 block/heading/range 模型
  - 与飞书 CLI 原生命令能力直接对应的技术常量
  - 通用文本规范化工具，但不能承载业务决策

  ———

  一、必须先完成的架构整改

  1. 重做 DocumentEditIntentResolver，去掉所有业务关键词硬编码
     目标文件：
     im-collab-ai-assistant-server/im-collab-ai-assistant-harness/src/main/java/com/lark/imcollab/harness/document/
     iteration/support/DocumentEditIntentResolver.java

     要求：
      - 只允许 LLM 或结构化 schema 产出 semanticAction + slots
      - 不允许本地代码再用“开头/章节/小节/正文/作者信息/顺序”之类词面二次猜语义
      - fallback 只能降级为：
          - 返回 unknown / unresolved
          - 要求审批或澄清
      - 不允许 fallback 里继续偷偷做 document_head、metadata、section_body 之类推断

     验收标准：
      - 删除所有业务词 contains(...)
      - 删除所有业务停用词过滤列表
      - 本地代码不再根据中文自然语言词面决定 DocumentSemanticActionType
  2. 彻底禁止“前插靠 block_replace 拼旧内容”
     目标文件：
     im-collab-ai-assistant-server/im-collab-ai-assistant-harness/src/main/java/com/lark/imcollab/harness/document/
     iteration/support/DocumentPatchCompiler.java

     必改点：
      - INSERT_METADATA_AT_DOCUMENT_HEAD
      - INSERT_SECTION_BEFORE_SECTION

     当前错误实现：
      - 生成 newContent = generated + anchorExcerpt
      - 再发 BLOCK_REPLACE

     必须改为：
      - 只生成合法结构策略
      - 优先用 block_insert_after + block_move_after 或其他与平台能力一致的受控两步法
      - 如果平台能力仍不足，明确进入 requiresApproval=true 或返回不支持
      - 绝不允许继续拼接原标题/原章节内容

     验收标准：
      - 代码中不再存在这两类语义对应的 BLOCK_REPLACE
      - 删除 buildBlockReplacePrependContent() 这类为旧路服务的实现
      - reasoning summary / justification 里也不能再写“受控 block_replace 前插”
  3. 让 DocumentPatchCompiler 真正基于新模型工作，移除对旧 DocumentTargetSelector 的主依赖
     目标文件：
     im-collab-ai-assistant-server/im-collab-ai-assistant-harness/src/main/java/com/lark/imcollab/harness/document/
     iteration/support/DocumentPatchCompiler.java
     im-collab-ai-assistant-server/im-collab-ai-assistant-common/src/main/java/com/lark/imcollab/common/model/entity/
     DocumentEditPlan.java

     要求：
      - patch compile 输入应直接使用：
          - DocumentEditIntent
          - ResolvedDocumentAnchor
          - DocumentEditStrategy
          - DocumentStructureSnapshot
      - DocumentTargetSelector 不能再承担：
          - preview
          - replace source
          - prompt source
          - verify source
      - matchedExcerpt 不能再是核心字段

     建议做法：
      - DocumentEditPlan 增加显式字段：
          - anchorSnapshot
          - promptContext
          - compiledPatchPlan
          - expectedState
      - selector 字段降级为兼容层，最终删除

     验收标准：
      - toSelector(...) 不再是编译主入口
      - 编译逻辑不再围绕 matchedExcerpt 展开
  4. 重做 DocumentAnchorResolver，禁止文本猜锚点作为主路径
     目标文件：
     im-collab-ai-assistant-server/im-collab-ai-assistant-harness/src/main/java/com/lark/imcollab/harness/document/
     iteration/support/DocumentAnchorResolver.java

     当前问题：
      - resolveOrdinalHeading()
      - matchHeadings()
      - plainText.contains(snippet)
      - 找不到就退到第一个 heading

     要求：
      - anchor resolution 必须先消费 intent slots
      - 使用快照中的结构索引解析 anchor
      - 若无法唯一解析，返回 unresolved，而不是猜一个最像的
      - 不允许“找不到就用第一个章节”

     验收标准：
      - 删除默认回退到 headings.get(0) 的逻辑
      - block anchor 不再通过 contains(snippet) 粗暴命中
      - section anchor 不能再依赖“第一小节/第三章”这种规则作为主决策链
  5. 把 DocumentStructureParser 从“业务规则中心”降级成“结构解析器”
     目标文件：
     im-collab-ai-assistant-server/im-collab-ai-assistant-harness/src/main/java/com/lark/imcollab/harness/document/
     iteration/support/DocumentStructureParser.java

     要求：
      - 保留：
          - heading/block/xml 解析
          - tag strip
          - block id 提取
      - 降级或移除：
          - resolveOrdinalHeading
          - “章节/部分/这一节”归一化替换
          - “小节”特殊逻辑
          - decimal heading 推断主路径

     原则：
      - parser 只做结构事实提取
      - 不做用户意图解释
      - 不做业务语义猜测

  ———

  二、必须重做的 verify 体系

  6. DocumentTargetStateVerifier 必须改成结构态校验，不允许只看全文 contains
     目标文件：
     im-collab-ai-assistant-server/im-collab-ai-assistant-harness/src/main/java/com/lark/imcollab/harness/document/
     iteration/support/DocumentTargetStateVerifier.java

     当前错误：
      - afterMarkdown.contains(firstMeaningfulLine(generated))
      - afterMarkdown.contains(preview)
      - 仍依赖 selector.matchedExcerpt

     必须改为按 ExpectedDocumentState 分类型校验：
      - EXPECT_METADATA_BLOCK_PRESENT_AT_HEAD
          - 校验 metadata block 位于首个 top-level section 之前
          - 校验原第一节 heading 未被复制/吞掉
      - EXPECT_NEW_SECTION_BEFORE_TARGET_SECTION
          - 校验新 section 与目标 section 的相对顺序
          - 校验目标 heading 仍然是原 block
      - EXPECT_SECTION_BODY_REMOVED
          - 校验 heading block 仍在
          - bodyBlockIds 全部消失
      - EXPECT_SECTION_REMOVED
          - 校验 allBlockIds 全部消失
      - EXPECT_TEXT_REPLACED
          - 校验目标 text anchor 处文本替换成功
          - 不是全篇任意地方出现新文本就算成功

     验收标准：
      - verifier 不再读取 selector.matchedExcerpt 作为最终依据
      - verifier 主要基于 before/after snapshot 的 block/section 结构比对
  7. DocumentPatchExecutor 的 patch-family verify 只能做底层防护，不能做架构中心
     目标文件：
     im-collab-ai-assistant-server/im-collab-ai-assistant-harness/src/main/java/com/lark/imcollab/harness/document/
     iteration/support/DocumentPatchExecutor.java

     要求：
      - 保留 revision 推进、block 落盘、API 返回校验
      - 但不能再把 verifyBlockReplace / verifyBlockInsert / verifyStringReplace 当最终成功判定
      - 最终成功判定必须以 DocumentTargetStateVerifier 为准

     验收标准：
      - DefaultDocumentIterationExecutionService 中的成功语义，以 target-state verify 为最终结论
      - patch executor 只负责“命令执行层”校验

  ———

  三、必须清理的旧架构残留

  8. 明确废弃旧三件套
     目标文件：
     im-collab-ai-assistant-server/im-collab-ai-assistant-harness/src/main/java/com/lark/imcollab/harness/document/
     iteration/support/DocumentTargetLocator.java
     im-collab-ai-assistant-server/im-collab-ai-assistant-harness/src/main/java/com/lark/imcollab/harness/document/
     iteration/support/DocumentAnchorIntentService.java
     im-collab-ai-assistant-server/im-collab-ai-assistant-harness/src/main/java/com/lark/imcollab/harness/document/
     iteration/support/DocumentEditPlanBuilder.java

     要求：
      - 不允许新链路再依赖：
          - LOCATOR_STRATEGY
          - RELATIVE_POSITION
          - LOCATOR_VALUE
      - 这些类要么删除，要么加 @Deprecated 并从 Spring 主装配移除
      - 测试也要迁移到新链路，不要再继续给旧 locator 修补测试
  9. 清理 DocumentEditPlan / VO 中的旧世界字段耦合
     目标文件：
     im-collab-ai-assistant-server/im-collab-ai-assistant-common/src/main/java/com/lark/imcollab/common/model/entity/
     DocumentEditPlan.java
     im-collab-ai-assistant-server/im-collab-ai-assistant-harness/src/main/java/com/lark/imcollab/harness/document/
     iteration/service/DefaultDocumentIterationExecutionService.java

     要求：
      - toPlanVO() 不再从 selector.locatorValue、selector.matchedExcerpt 取值
      - 对外展示应该来自：
          - resolvedAnchor
          - semanticAction
          - strategyType
          - expectedState
      - toolCommandType 可以保留做调试信息，但不能再是前端主视角

  ———

  四、硬编码专项禁令

  10. 全项目扫描并清除文档迭代链路中的业务硬编码

     扫描范围：
     im-collab-ai-assistant-harness/src/main/java/com/lark/imcollab/harness/document/iteration

  必须清理的模式：

  - contains("开头")
  - contains("文首")
  - contains("最前面")
  - contains("章节")
  - contains("小节")
  - contains("正文")
  - contains("作者")
  - contains("作者信息")
  - contains("顺序")
  - equals("小节")
  - equals("章节")
  - equals("document_head")
  - List.of("删除", "开头", ...) 这类业务停用词表
  - 任何按中文词面决定 semanticAction / anchorType / strategyType / expectedState 的实现

  允许的例外：

  - LLM prompt 示例文本
  - 异常消息文案
  - API/CLI 固定协议枚举值
  - 通用 Markdown/heading 语法判断

  11. 禁止“假 fallback”

     要求：

  - fallback 不是偷偷继续猜
  - fallback 只能是：
      - unresolved
      - clarification
      - approval required
      - unsupported by platform
  - 绝不允许“猜不到就选第一个章节/默认文首/默认正文”

  ———
  5. 最后清理 DocumentTargetLocator / DocumentAnchorIntentService / DocumentEditPlanBuilder
  6. 最后补测试，确保旧路径不再被引用

  ———

  六、交付验收标准

  Claude 改完后，至少要满足以下标准：

  1. 任意文档迭代请求，本地 Java 代码不再靠中文关键词硬编码决定业务语义。
  2. “文首插入 metadata” 与 “章节前插 section” 不再使用 block_replace 拼接旧内容。
  3. 新链路不再以 DocumentTargetSelector / matchedExcerpt 为核心。
  4. 无法唯一定位时返回 unresolved/approval，不允许猜测性落点。
  5. verify 以 before/after snapshot 的结构对比为主，不再以全文 contains 为主。
  6. 旧 locator/plan builder 架构不再处于可复用主路径。
  7. 文档迭代相关代码中，不再出现你禁止的那类硬编码垃圾逻辑。