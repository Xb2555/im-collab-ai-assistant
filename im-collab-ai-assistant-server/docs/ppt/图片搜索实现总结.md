# AI 图片搜索功能实现总结

## 1. 核心原理

**AI 不是浏览器，不会上网搜索**。真实流程是：

```
用户需求 → AI（大模型）分析意图 → 生成图片计划(JSON) → 调用工具 → 工具调用外部图片API → 返回图片URL
```

AI 在整个流程中充当**编排器（Orchestrator）**，真正的图片搜索由 `HttpRequest` 调用外部 API 完成。

---

## 2. 整体架构

```
ImageCollectorNode（编排节点）
  ├── ImageCollectionPlanService → AI分析生成计划（JSON）
  │     └── image-collection-plan-system-prompt.txt（提示词）
  │
  └── 并发执行四类任务
        ├── ImageSearchTool.searchContentImages() → Pexels API（配图）
        ├── UndrawIllustrationTool.searchIllustrations() → Undraw API（插画）
        ├── MermaidDiagramTool.generateMermaidDiagram() → Mermaid CLI（架构图）
        └── LogoGeneratorTool.generateLogos() → DashScope API（AI生成Logo）
```

---

## 3. 核心代码

### 3.1 图片资源模型

```java
// ImageResource.java
@Data
@Builder
public class ImageResource implements Serializable {
    private ImageCategoryEnum category;  // 分类：CONTENT / ILLUSTRATION / LOGO / DIAGRAM
    private String description;           // 图片描述
    private String url;                   // 图片URL
}
```

### 3.2 图片收集计划模型

```java
// ImageCollectionPlan.java
@Data
public class ImageCollectionPlan implements Serializable {
    private List<ImageSearchTask> contentImageTasks;    // 配图任务
    private List<IllustrationTask> illustrationTasks;   // 插画任务
    private List<DiagramTask> diagramTasks;            // 架构图任务
    private List<LogoTask> logoTasks;                  // Logo任务

    public record ImageSearchTask(String query) {}
    public record IllustrationTask(String query) {}
    public record DiagramTask(String mermaidCode, String description) {}
    public record LogoTask(String description) {}
}
```

### 3.3 计划生成服务（用提示词让 AI 生成 JSON 计划）

```java
// ImageCollectionPlanService.java
public interface ImageCollectionPlanService {
    @SystemMessage(fromResource = "prompt/image-collection-plan-system-prompt.txt")
    ImageCollectionPlan planImageCollection(@UserMessage String userPrompt);
}
```

### 3.4 Pexels 搜索配图（最核心的搜索功能）

```java
// ImageSearchTool.java
@Component
public class ImageSearchTool {
    private static final String PEXELS_API_URL = "https://api.pexels.com/v1/search";

    @Value("${pexels.api-key}")
    private String pexelsApiKey;

    @Tool("搜索内容相关的图片，用于网站内容展示")
    public List<ImageResource> searchContentImages(@P("搜索关键词") String query) {
        List<ImageResource> imageList = new ArrayList<>();
        int searchCount = 12;
        try (HttpResponse response = HttpRequest.get(PEXELS_API_URL)
                .header("Authorization", pexelsApiKey)
                .form("query", query)
                .form("per_page", searchCount)
                .form("page", 1)
                .execute()) {
            if (response.isOk()) {
                JSONObject result = JSONUtil.parseObj(response.body());
                JSONArray photos = result.getJSONArray("photos");
                for (int i = 0; i < photos.size(); i++) {
                    JSONObject photo = photos.getJSONObject(i);
                    JSONObject src = photo.getJSONObject("src");
                    imageList.add(ImageResource.builder()
                            .category(ImageCategoryEnum.CONTENT)
                            .description(photo.getStr("alt", query))
                            .url(src.getStr("medium"))
                            .build());
                }
            }
        } catch (Exception e) {
            log.error("Pexels API 调用失败: {}", e.getMessage(), e);
        }
        return imageList;
    }
}
```

### 3.5 Undraw 搜索插画

```java
// UndrawIllustrationTool.java
@Component
public class UndrawIllustrationTool {
    private static final String UNDRAW_API_URL = "https://undraw.co/_next/data/mMWmJSt23qpgo8cLTD_pB/search/%s.json?term=%s";

    @Tool("搜索插画图片，用于网站美化和装饰")
    public List<ImageResource> searchIllustrations(@P("搜索关键词") String query) {
        List<ImageResource> imageList = new ArrayList<>();
        String apiUrl = String.format(UNDRAW_API_URL, query, query);
        try (HttpResponse response = HttpRequest.get(apiUrl).timeout(10000).execute()) {
            if (!response.isOk()) return imageList;
            JSONObject result = JSONUtil.parseObj(response.body());
            JSONArray initialResults = result.getJSONObject("pageProps").getJSONArray("initialResults");
            for (int i = 0; i < Math.min(12, initialResults.size()); i++) {
                JSONObject illustration = initialResults.getJSONObject(i);
                imageList.add(ImageResource.builder()
                        .category(ImageCategoryEnum.ILLUSTRATION)
                        .description(illustration.getStr("title", "插画"))
                        .url(illustration.getStr("media"))
                        .build());
            }
        } catch (Exception e) {
            log.error("搜索插画失败：{}", e.getMessage(), e);
        }
        return imageList;
    }
}
```

### 3.6 阿里通义万相生成 Logo

```java
// LogoGeneratorTool.java
@Component
public class LogoGeneratorTool {
    @Value("${dashscope.api-key:}")
    private String dashScopeApiKey;

    @Value("${dashscope.image-model:wan2.2-t2i-flash}")
    private String imageModel;

    @Tool("根据描述生成 Logo 设计图片")
    public List<ImageResource> generateLogos(@P("Logo设计描述") String description) {
        List<ImageResource> logoList = new ArrayList<>();
        try {
            String logoPrompt = String.format("生成 Logo，Logo 中禁止包含任何文字！Logo 介绍：%s", description);
            ImageSynthesisParam param = ImageSynthesisParam.builder()
                    .apiKey(dashScopeApiKey)
                    .model(imageModel)
                    .prompt(logoPrompt)
                    .size("512*512")
                    .n(1)
                    .build();
            ImageSynthesisResult result = new ImageSynthesis().call(param);
            // 解析 result.getOutput().getResults() 获取图片 URL
        } catch (Exception e) {
            log.error("生成 Logo 失败: {}", e.getMessage(), e);
        }
        return logoList;
    }
}
```

### 3.7 编排节点（并发执行所有图片任务）

```java
// ImageCollectorNode.java
public class ImageCollectorNode {
    public static AsyncNodeAction<MessagesState<String>> create() {
        return node_async(state -> {
            WorkflowContext context = WorkflowContext.getContext(state);
            List<ImageResource> collectedImages = new ArrayList<>();

            // 第一步：AI 生成图片计划
            ImageCollectionPlanService planService = SpringContextUtil.getBean(ImageCollectionPlanService.class);
            ImageCollectionPlan plan = planService.planImageCollection(context.getOriginalPrompt());

            // 第二步：并发执行所有图片任务
            List<CompletableFuture<List<ImageResource>>> futures = new ArrayList<>();

            if (plan.getContentImageTasks() != null) {
                ImageSearchTool tool = SpringContextUtil.getBean(ImageSearchTool.class);
                for (ImageCollectionPlan.ImageSearchTask task : plan.getContentImageTasks()) {
                    futures.add(CompletableFuture.supplyAsync(() ->
                            tool.searchContentImages(task.query())));
                }
            }
            if (plan.getIllustrationTasks() != null) {
                UndrawIllustrationTool tool = SpringContextUtil.getBean(UndrawIllustrationTool.class);
                for (ImageCollectionPlan.IllustrationTask task : plan.getIllustrationTasks()) {
                    futures.add(CompletableFuture.supplyAsync(() ->
                            tool.searchIllustrations(task.query())));
                }
            }
            // ... diagramTasks, logoTasks 同理

            CompletableFuture.allOf(futures.toArray(new CompletableFuture[0])).join();
            for (CompletableFuture<List<ImageResource>> f : futures) {
                collectedImages.addAll(f.get());
            }
            context.setImageList(collectedImages);
            return WorkflowContext.saveContext(context);
        });
    }
}
```

---

## 4. 提示词模板

### 4.1 图片计划生成提示词（决定搜什么图）

```txt
# image-collection-plan-system-prompt.txt

你是一个专业的图片收集规划师。你的任务是分析用户的网站需求，制定合理的图片收集计划。

## 图片类型说明
1. contentImageTasks - 内容配图，通过关键词搜索获取
2. illustrationTasks - 装饰插画，从 Undraw 插画库获取
3. diagramTasks - 架构图，通过 Mermaid 代码生成
4. logoTasks - Logo，AI 生成

## 输出格式（必须严格遵循）
{
  "contentImageTasks": [{ "query": "搜索关键词" }],
  "illustrationTasks": [{ "query": "插画关键词" }],
  "diagramTasks": [{ "mermaidCode": "mermaid代码", "description": "描述" }],
  "logoTasks": [{ "description": "Logo描述" }]
}
```

### 4.2 工具说明提示词（让 AI 知道有哪些工具可用）

```txt
# image-collection-system-prompt.txt

你是一个专业的图片收集助手。可以调用以下工具：
1. searchContentImages - 搜索内容相关图片
2. searchIllustrations - 搜索插画图片
3. generateArchitectureDiagram - 生成架构图
4. generateLogos - 生成Logo

你必须按照 JSON 格式输出！
```

---

## 5. 依赖配置

```yaml
# application.yml 或 application.properties
pexels.api-key: 你的Pexels_API密钥
dashscope.api-key: 你的阿里云DashScope密钥
dashscope.image-model: wan2.2-t2i-flash
```

### Maven 依赖（关键部分）

```xml
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j</artifactId>
</dependency>
<dependency>
    <groupId>cn.hutool</groupId>
    <artifactId>hutool-http</artifactId>
</dependency>
<dependency>
    <groupId>com.alibaba</groupId>
    <artifactId>dashscope</artifactId>
</dependency>
```

---

## 6. 在其他项目中复现的最小步骤

1. **引入依赖**：hutool-http + langchain4j + dashscope（可选）
2. **创建 ImageResource 模型**：存储图片 URL 和描述
3. **创建 ImageSearchTool**：用 Hutool 的 `HttpRequest` 调用 Pexels API，返回 `List<ImageResource>`
4. **可选：添加 UndrawIllustrationTool**：同理调用 Undraw API
5. **可选：添加 LogoGeneratorTool**：调用通义万相 API 生成 Logo
6. **调用工具**：直接传入关键词字符串即可，AI 编排是可选的

**如果只想要"给定关键词 → 返回图片列表"的功能，只需要 3 个文件**：

```
ImageResource.java          # 数据模型
ImageSearchTool.java       # 调用 Pexels API
ImageSearchService.java     # 对外暴露的搜索服务
```

API 密钥申请：
- **Pexels**: https://www.pexels.com/api/ （免费账号每月 200 次）
- **Undraw**: 免费，无需 API Key
- **通义万相**: https://dashscope.console.aliyun.com/ （阿里云百炼平台）
